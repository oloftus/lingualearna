(function() {

    var imports = [ "linguaApp" ];

    define(imports, function(linguaApp) {

        Subjects = {
            TRANSLATION_REQUEST : "translation-request",
            Note : "note"
        };
    });
})();
